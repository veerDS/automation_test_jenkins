{
  "name": "run-testng-action",
  "version": "1.0.0",
  "description": "GitHub Action — runs TestNG suite and exposes HTML report outputs for email",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "ncc build index.js -o dist/main",
    "build:watch": "ncc build index.js -o dist/main --watch"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}